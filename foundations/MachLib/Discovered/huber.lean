import MachLib.Basic
-- Generated by the EML-lang Lean backend
-- Source module: huber_loss
-- Source file:   <private>/huber.eml
-- Verified fns:  huber_loss, huber_grad

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def MAX_RESIDUAL : Real := (1000000.0 : Real)
noncomputable def HALF : Real := (0.5 : Real)

axiom smooth_l1 (prediction : Real) (target : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── huber_loss ──

noncomputable def huber_loss (prediction : Real) (target : Real) (delta : Real) : Real :=
  (((HALF * (min (max (abs (prediction - target)) (0.0 : Real)) delta)) * (min (max (abs (prediction - target)) (0.0 : Real)) delta)) + (delta * ((abs (prediction - target)) - (min (max (abs (prediction - target)) (0.0 : Real)) delta))))

theorem huber_loss_nonnegative (prediction : Real) (target : Real) (delta : Real)
    (h1 : ((abs prediction) < MAX_RESIDUAL))
    (h2 : ((abs target) < MAX_RESIDUAL))
    (h3 : (delta > (0.0 : Real)))
    (h4 : (delta <= MAX_RESIDUAL)) :
    ((huber_loss prediction target delta) >= (0.0 : Real)) := by
  unfold huber_loss
  sorry  -- TODO: prove against MachLib foundations

-- ── huber_grad ──

noncomputable def huber_grad (prediction : Real) (target : Real) (delta : Real) : Real :=
  (min (max (prediction - target) (-delta)) delta)

theorem huber_grad_bounded_by_delta (prediction : Real) (target : Real) (delta : Real)
    (h1 : ((abs prediction) < MAX_RESIDUAL))
    (h2 : ((abs target) < MAX_RESIDUAL))
    (h3 : (delta > (0.0 : Real)))
    (h4 : (delta <= MAX_RESIDUAL)) :
    True := by
  trivial
