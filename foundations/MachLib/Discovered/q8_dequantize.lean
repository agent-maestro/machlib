-- Generated by the EML-lang Lean backend
-- Source module: q8_dequantize
-- Source file:   <private>/q8_dequantize.eml
-- Verified fns:  q8_dequantize, q8_dequantize_asym, matmul_rescale

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def SCALE_MIN : Real := (1e-09 : Real)
noncomputable def SCALE_MAX : Real := (1000000000.0 : Real)
noncomputable def ACC_MIN : Real := (-(2147483648.0 : Real))
noncomputable def ACC_MAX : Real := (2147483647.0 : Real)

-- ── q8_dequantize ──

noncomputable def q8_dequantize (q : Real) (scale : Real) : Real :=
  (q * scale)

theorem q8_dequantize_inverse_of_quantize (q : Real) (scale : Real)
    (h1 : (scale >= SCALE_MIN))
    (h2 : (scale <= SCALE_MAX)) :
    True := by
  trivial

-- ── q8_dequantize_asym ──

noncomputable def q8_dequantize_asym (q : Real) (scale : Real) (zero_point : Real) : Real :=
  ((q - zero_point) * scale)

theorem q8_dequantize_asym_inverse (q : Real) (scale : Real) (zero_point : Real)
    (h1 : (scale >= SCALE_MIN))
    (h2 : (scale <= SCALE_MAX))
    (h3 : (zero_point >= (-(128.0 : Real))))
    (h4 : (zero_point <= (127.0 : Real))) :
    True := by
  trivial

-- ── matmul_rescale ──

noncomputable def matmul_rescale (accum : Real) (input_scale : Real) (weight_scale : Real) : Real :=
  ((accum * input_scale) * weight_scale)

theorem matmul_rescale_preserves_sign (accum : Real) (input_scale : Real) (weight_scale : Real)
    (h1 : (input_scale >= SCALE_MIN))
    (h2 : (input_scale <= SCALE_MAX))
    (h3 : (weight_scale >= SCALE_MIN))
    (h4 : (weight_scale <= SCALE_MAX))
    (h5 : (accum >= ACC_MIN))
    (h6 : (accum <= ACC_MAX)) :
    True := by
  trivial
