-- Generated by the EML-lang Lean backend
-- Source module: stefan_boltzmann
-- Source file:   <private>/stefan_boltzmann.eml
-- Verified fns:  radiative_flux, net_exchange, wien_peak_wavelength

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def SIGMA_SB : Real := (5.670374419e-08 : Real)
noncomputable def T_MIN : Real := (1 : Real)
noncomputable def T_MAX : Real := (6000.0 : Real)
noncomputable def EPS_MIN : Real := (0 : Real)
noncomputable def EPS_MAX : Real := (1 : Real)

-- ── radiative_flux ──

noncomputable def radiative_flux (emissivity : Real) (temperature_k : Real) : Real :=
  ((emissivity * SIGMA_SB) * (temperature_k ^ (4.0 : Real)))

theorem stefan_boltzmann_monotone_in_T (emissivity : Real) (temperature_k : Real)
    (h1 : (emissivity >= EPS_MIN))
    (h2 : (emissivity <= EPS_MAX))
    (h3 : (temperature_k >= T_MIN))
    (h4 : (temperature_k <= T_MAX)) :
    ((radiative_flux emissivity temperature_k) >= (0 : Real)) := by
  unfold radiative_flux
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover

-- ── net_exchange ──

noncomputable def net_exchange (emissivity : Real) (surface_t : Real) (ambient_t : Real) : Real :=
  ((emissivity * SIGMA_SB) * ((surface_t ^ (4.0 : Real)) - (ambient_t ^ (4.0 : Real))))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem net_exchange_zero_at_equilibrium (emissivity : Real) (surface_t : Real) (ambient_t : Real)
    (h1 : (emissivity >= EPS_MIN))
    (h2 : (emissivity <= EPS_MAX))
    (h3 : (surface_t >= T_MIN))
    (h4 : (surface_t <= T_MAX))
    (h5 : (ambient_t >= T_MIN))
    (h6 : (ambient_t <= T_MAX)) :
    True := by
  trivial

-- ── wien_peak_wavelength ──

noncomputable def wien_peak_wavelength (temperature_k : Real) : Real :=
  ((0.002897771955 : Real) / temperature_k)

theorem wien_inversely_proportional_to_T (temperature_k : Real)
    (h1 : (temperature_k >= T_MIN))
    (h2 : (temperature_k <= T_MAX)) :
    ((wien_peak_wavelength temperature_k) > (0 : Real)) := by
  unfold wien_peak_wavelength
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover
