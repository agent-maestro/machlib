-- Generated by the EML-lang Lean backend
-- Source module: spectrum_peak
-- Source file:   <private>/spectrum_peak.eml
-- Verified fns:  magnitude_squared, log_magnitude_db, spectrum_detect

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def COMPONENT_MAX : Real := (1000000.0 : Real)
noncomputable def POWER_MIN : Real := (1e-30 : Real)
noncomputable def POWER_MAX : Real := (1e+30 : Real)
noncomputable def LN10_INV : Real := (0.4342944819032518 : Real)

-- ── magnitude_squared ──

noncomputable def magnitude_squared (real_part : Real) (imag_part : Real) : Real :=
  ((real_part * real_part) + (imag_part * imag_part))

theorem magnitude_squared_nonneg (real_part : Real) (imag_part : Real)
    (h1 : ((abs real_part) <= COMPONENT_MAX))
    (h2 : ((abs imag_part) <= COMPONENT_MAX)) :
    ((magnitude_squared real_part imag_part) >= (0 : Real)) := by
  unfold magnitude_squared
  sorry  -- TODO: prove against MachLib foundations

-- ── log_magnitude_db ──

noncomputable def log_magnitude_db (power : Real) : Real :=
  (((10.0 : Real) * LN10_INV) * (Real.log power))

theorem log_magnitude_db_finite_above_floor (power : Real)
    (h1 : (power >= POWER_MIN))
    (h2 : (power <= POWER_MAX)) :
    True := by
  trivial

-- ── spectrum_detect ──

noncomputable def spectrum_detect (power : Real) (noise_floor : Real) (margin : Real) : Real :=
  ((min (max (power - (margin * noise_floor)) (0 : Real)) (1 : Real)) / (min (max (power - (margin * noise_floor)) (1e-30 : Real)) (1 : Real)))

theorem spectrum_detect_zero_below_threshold (power : Real) (noise_floor : Real) (margin : Real)
    (h1 : (power >= (0 : Real)))
    (h2 : (power <= POWER_MAX))
    (h3 : (noise_floor >= (0 : Real)))
    (h4 : (noise_floor <= POWER_MAX))
    (h5 : (margin >= (1 : Real)))
    (h6 : (margin <= (1000000.0 : Real))) :
    ((spectrum_detect power noise_floor margin) >= (0 : Real)) := by
  unfold spectrum_detect
  sorry  -- TODO: prove against MachLib foundations
