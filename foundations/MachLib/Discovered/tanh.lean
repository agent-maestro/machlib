import MachLib.Basic
-- Generated by the EML-lang Lean backend
-- Source module: tanh_activation
-- Source file:   <private>/tanh.eml
-- Verified fns:  tanh_activation, tanh_from_sigmoid, hard_tanh

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def TANH_X_MAX : Real := (30.0 : Real)

-- ── tanh_activation ──

noncomputable def tanh_activation (x : Real) : Real :=
  (Real.tanh x)

theorem tanh_monotone_in_x (x : Real)
    (h1 : ((abs x) < TANH_X_MAX)) :
    ((tanh_activation x) >= (-(1.0 : Real))) := by
  unfold tanh_activation
  sorry  -- TODO: prove against MachLib foundations

-- ── tanh_from_sigmoid ──

noncomputable def tanh_from_sigmoid (x : Real) : Real :=
  (((2.0 : Real) / ((1.0 : Real) + (Real.exp ((-2.0 : Real) * x)))) - (1.0 : Real))

theorem tanh_from_sigmoid_equals_canonical (x : Real)
    (h1 : ((abs x) < TANH_X_MAX)) :
    ((tanh_from_sigmoid x) >= (-(1.0 : Real))) := by
  unfold tanh_from_sigmoid
  sorry  -- TODO: prove against MachLib foundations

-- ── hard_tanh ──

noncomputable def hard_tanh (x : Real) : Real :=
  (min (max x (-1.0 : Real)) (1.0 : Real))

theorem hard_tanh_bounded (x : Real) :
    ((hard_tanh x) >= (-(1.0 : Real))) := by
  unfold hard_tanh
  sorry  -- TODO: prove against MachLib foundations
