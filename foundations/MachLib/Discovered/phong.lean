-- Generated by the EML-lang Lean backend
-- Source module: phong
-- Source file:   <private>/phong.eml
-- Verified fns:  ambient, diffuse, specular, shade

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def ZERO : Real := (0 : Real)
noncomputable def ONE : Real := (1 : Real)
noncomputable def SHININESS_MIN : Real := (1 : Real)
noncomputable def SHININESS_MAX : Real := (1024.0 : Real)

-- ── ambient ──

noncomputable def ambient (k_ambient : Real) (light_ambient : Real) : Real :=
  (k_ambient * light_ambient)

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem phong_ambient_below_input (k_ambient : Real) (light_ambient : Real)
    (h1 : (k_ambient >= (0 : Real)))
    (h2 : (k_ambient <= ONE))
    (h3 : (light_ambient >= (0 : Real)))
    (h4 : (light_ambient <= ONE)) :
    True := by
  trivial

-- ── diffuse ──

noncomputable def diffuse (k_diffuse : Real) (n_dot_l_clamped : Real) (light_diffuse : Real) : Real :=
  ((k_diffuse * n_dot_l_clamped) * light_diffuse)

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem phong_diffuse_below_kd_times_id (k_diffuse : Real) (n_dot_l_clamped : Real) (light_diffuse : Real)
    (h1 : (k_diffuse >= (0 : Real)))
    (h2 : (k_diffuse <= ONE))
    (h3 : (n_dot_l_clamped >= (0 : Real)))
    (h4 : (n_dot_l_clamped <= ONE))
    (h5 : (light_diffuse >= (0 : Real)))
    (h6 : (light_diffuse <= ONE)) :
    True := by
  trivial

-- ── specular ──

noncomputable def specular (k_specular : Real) (r_dot_v_clamped : Real) (shininess : Real) (light_specular : Real) : Real :=
  ((k_specular * (r_dot_v_clamped ^ shininess)) * light_specular)

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem phong_specular_below_ks_times_is (k_specular : Real) (r_dot_v_clamped : Real) (shininess : Real) (light_specular : Real)
    (h1 : (k_specular >= (0 : Real)))
    (h2 : (k_specular <= ONE))
    (h3 : (r_dot_v_clamped >= (0 : Real)))
    (h4 : (r_dot_v_clamped <= ONE))
    (h5 : (shininess >= SHININESS_MIN))
    (h6 : (shininess <= SHININESS_MAX))
    (h7 : (light_specular >= (0 : Real)))
    (h8 : (light_specular <= ONE)) :
    True := by
  trivial

-- ── shade ──

noncomputable def shade (ambient_i : Real) (diffuse_i : Real) (specular_i : Real) : Real :=
  ((ambient_i + diffuse_i) + specular_i)

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem phong_total_above_each_component (ambient_i : Real) (diffuse_i : Real) (specular_i : Real)
    (h1 : (ambient_i >= (0 : Real)))
    (h2 : (diffuse_i >= (0 : Real)))
    (h3 : (specular_i >= (0 : Real))) :
    True := by
  trivial
