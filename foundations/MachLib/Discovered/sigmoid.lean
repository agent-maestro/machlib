import MachLib.Basic
-- Generated by the EML-lang Lean backend
-- Source module: sigmoid
-- Source file:   <private>/sigmoid.eml
-- Verified fns:  sigmoid, sigmoid_alt

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def SIGMOID_X_MAX : Real := (30.0 : Real)
noncomputable def HALF : Real := (0.5 : Real)

axiom logit (p : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── sigmoid ──

noncomputable def sigmoid (x : Real) : Real :=
  ((1.0 : Real) / ((1.0 : Real) + (Real.exp (-x))))

theorem sigmoid_monotone_in_x (x : Real)
    (h1 : ((abs x) < SIGMOID_X_MAX)) :
    ((sigmoid x) >= (0.0 : Real)) := by
  unfold sigmoid
  sorry  -- TODO: prove against MachLib foundations

-- ── sigmoid_alt ──

noncomputable def sigmoid_alt (x : Real) : Real :=
  (((Real.tanh (x * HALF)) * HALF) + HALF)

theorem sigmoid_alt_equals_canonical (x : Real)
    (h1 : ((abs x) < SIGMOID_X_MAX)) :
    ((sigmoid_alt x) >= (0.0 : Real)) := by
  unfold sigmoid_alt
  sorry  -- TODO: prove against MachLib foundations
