import MachLib.Basic
-- Generated by the EML-lang Lean backend
-- Source module: crystallization
-- Source file:   <private>/crystallization.eml
-- Verified fns:  relative_supersaturation, absolute_supersaturation, power_law_growth_rate

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def C_MAX : Real := (1000000.0 : Real)
noncomputable def KG_MAX : Real := (1000.0 : Real)
noncomputable def G_MAX : Real := (5.0 : Real)

-- ── relative_supersaturation ──

noncomputable def relative_supersaturation (concentration : Real) (saturation_concentration : Real) : Real :=
  (concentration / saturation_concentration)

theorem supersaturation_zero_at_solubility (concentration : Real) (saturation_concentration : Real)
    (h1 : (concentration >= (0.0 : Real)))
    (h2 : (saturation_concentration > (0.0 : Real))) :
    (((relative_supersaturation concentration saturation_concentration) >= (0.0 : Real)) ∨ (concentration < saturation_concentration)) := by
  unfold relative_supersaturation
  sorry  -- TODO: prove against MachLib foundations

-- ── absolute_supersaturation ──

noncomputable def absolute_supersaturation (concentration : Real) (saturation_concentration : Real) : Real :=
  ((concentration - saturation_concentration) / saturation_concentration)

theorem absolute_supersaturation_zero_at_solubility (concentration : Real) (saturation_concentration : Real)
    (h1 : (concentration >= (0.0 : Real)))
    (h2 : (saturation_concentration > (0.0 : Real))) :
    True := by
  trivial

-- ── power_law_growth_rate ──

noncomputable def power_law_growth_rate (growth_constant : Real) (concentration : Real) (saturation_concentration : Real) (growth_exponent : Real) : Real :=
  (growth_constant * ((max (concentration - saturation_concentration) (0.0 : Real)) ^ growth_exponent))

theorem growth_rate_nonneg_above_solubility (growth_constant : Real) (concentration : Real) (saturation_concentration : Real) (growth_exponent : Real)
    (h1 : (growth_constant >= (0.0 : Real)))
    (h2 : (growth_constant <= KG_MAX))
    (h3 : (concentration >= (0.0 : Real)))
    (h4 : (saturation_concentration > (0.0 : Real)))
    (h5 : (growth_exponent > (0.0 : Real)))
    (h6 : (growth_exponent <= G_MAX)) :
    ((power_law_growth_rate growth_constant concentration saturation_concentration growth_exponent) >= (0.0 : Real)) := by
  unfold power_law_growth_rate
  sorry  -- TODO: prove against MachLib foundations
