import MachLib.Basic
-- Generated by the EML-lang Lean backend
-- Source module: basel_capital
-- Source file:   <private>/basel_capital.eml
-- Verified fns:  frtb_capital_charge, ima_total_capital, diversification_pair, risk_aggregate_finalize

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def CAP_MAX : Real := (1000000000000.0 : Real)
noncomputable def RHO_MIN : Real := (-(1.0 : Real))
noncomputable def RHO_MAX : Real := (1.0 : Real)
noncomputable def MC_MIN : Real := (1.0 : Real)
noncomputable def MC_MAX : Real := (5.0 : Real)

-- ── frtb_capital_charge ──

noncomputable def frtb_capital_charge (var_average : Real) (var_max : Real) (multiplier : Real) : Real :=
  (multiplier * (min (max var_max var_average) CAP_MAX))

theorem capital_max_dominates_average (var_average : Real) (var_max : Real) (multiplier : Real)
    (h1 : (var_average >= (0.0 : Real)))
    (h2 : (var_average <= CAP_MAX))
    (h3 : (var_max >= (0.0 : Real)))
    (h4 : (var_max <= CAP_MAX))
    (h5 : (multiplier >= MC_MIN))
    (h6 : (multiplier <= MC_MAX)) :
    True := by
  trivial

-- ── ima_total_capital ──

noncomputable def ima_total_capital (capital_unstressed : Real) (capital_stressed : Real) : Real :=
  (Real.sqrt ((capital_unstressed * capital_unstressed) + (capital_stressed * capital_stressed)))

theorem stressed_unstressed_pythagorean (capital_unstressed : Real) (capital_stressed : Real)
    (h1 : (capital_unstressed >= (0.0 : Real)))
    (h2 : (capital_unstressed <= CAP_MAX))
    (h3 : (capital_stressed >= (0.0 : Real)))
    (h4 : (capital_stressed <= CAP_MAX)) :
    ((ima_total_capital capital_unstressed capital_stressed) >= (0.0 : Real)) := by
  unfold ima_total_capital
  sorry  -- TODO: prove against MachLib foundations

-- ── diversification_pair ──

noncomputable def diversification_pair (cap_i : Real) (cap_j : Real) (rho : Real) : Real :=
  ((rho * cap_i) * cap_j)

theorem diversification_pair_signed_by_rho (cap_i : Real) (cap_j : Real) (rho : Real)
    (h1 : (cap_i >= (0.0 : Real)))
    (h2 : (cap_i <= CAP_MAX))
    (h3 : (cap_j >= (0.0 : Real)))
    (h4 : (cap_j <= CAP_MAX))
    (h5 : (rho >= RHO_MIN))
    (h6 : (rho <= RHO_MAX)) :
    True := by
  trivial

-- ── risk_aggregate_finalize ──

noncomputable def risk_aggregate_finalize (accumulated_sum : Real) : Real :=
  (Real.sqrt accumulated_sum)

theorem risk_finalize_nonneg (accumulated_sum : Real)
    (h1 : (accumulated_sum >= (0.0 : Real)))
    (h2 : (accumulated_sum <= (CAP_MAX * CAP_MAX))) :
    ((risk_aggregate_finalize accumulated_sum) >= (0.0 : Real)) := by
  unfold risk_aggregate_finalize
  sorry  -- TODO: prove against MachLib foundations
