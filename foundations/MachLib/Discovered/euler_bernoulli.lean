-- Generated by the EML-lang Lean backend
-- Source module: euler_bernoulli
-- Source file:   <private>/euler_bernoulli.eml
-- Verified fns:  deflection_simple_center, deflection_simple_udl, deflection_cantilever_end, bending_stress

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def ONE : Real := (1 : Real)
noncomputable def THREE : Real := (3.0 : Real)
noncomputable def FOURTY_EIGHT : Real := (48.0 : Real)
noncomputable def FIVE : Real := (5.0 : Real)
noncomputable def THREE_EIGHTY_FOUR : Real := (384.0 : Real)
noncomputable def F_MAX : Real := (100000000.0 : Real)
noncomputable def W_MAX : Real := (1000000.0 : Real)
noncomputable def L_MIN : Real := (0.1 : Real)
noncomputable def L_MAX : Real := (1000.0 : Real)
noncomputable def E_MIN : Real := (100000000.0 : Real)
noncomputable def E_MAX : Real := (1000000000000.0 : Real)
noncomputable def I_MIN : Real := (1e-12 : Real)
noncomputable def I_MAX : Real := (1 : Real)
noncomputable def C_MAX : Real := (10.0 : Real)
noncomputable def M_MAX : Real := (10000000000.0 : Real)

-- ── deflection_simple_center ──

noncomputable def deflection_simple_center (point_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real) : Real :=
  ((((point_load * span_length) * span_length) * span_length) / ((FOURTY_EIGHT * modulus) * second_moment))

-- source obligations for deflection_simple_center: {O1}
--   O1 [da7bb58b74f3] -> PRESERVED (accounted)  theorem ssb_center_load_deflection_nonneg
--        build: unconditional -- the theorem STATEMENT is in the artifact unconditionally; whether it is PROVED is a separate axis -- this checker rejects an undischarged theorem unless cheating is explicitly enabled
theorem ssb_center_load_deflection_nonneg (point_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real)
    (h1 : (point_load >= (0 : Real)))
    (h2 : (point_load <= F_MAX))
    (h3 : (span_length >= L_MIN))
    (h4 : (span_length <= L_MAX))
    (h5 : (modulus >= E_MIN))
    (h6 : (modulus <= E_MAX))
    (h7 : (second_moment >= I_MIN))
    (h8 : (second_moment <= I_MAX)) :
    ((deflection_simple_center point_load span_length modulus second_moment) >= (0 : Real)) := by
  unfold deflection_simple_center
  try unfold ONE at *
  try unfold THREE at *
  try unfold FOURTY_EIGHT at *
  try unfold FIVE at *
  try unfold THREE_EIGHTY_FOUR at *
  try unfold F_MAX at *
  try unfold W_MAX at *
  try unfold L_MIN at *
  try unfold L_MAX at *
  try unfold E_MIN at *
  try unfold E_MAX at *
  try unfold I_MIN at *
  try unfold I_MAX at *
  try unfold C_MAX at *
  try unfold M_MAX at *
  try mach_split_hyps
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover

-- ── deflection_simple_udl ──

noncomputable def deflection_simple_udl (distributed_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real) : Real :=
  ((((((FIVE * distributed_load) * span_length) * span_length) * span_length) * span_length) / ((THREE_EIGHTY_FOUR * modulus) * second_moment))

-- source obligations for deflection_simple_udl: {O1}
--   O1 [a92ed3c21955] -> PRESERVED (accounted)  theorem ssb_udl_deflection_nonneg
--        build: unconditional -- the theorem STATEMENT is in the artifact unconditionally; whether it is PROVED is a separate axis -- this checker rejects an undischarged theorem unless cheating is explicitly enabled
theorem ssb_udl_deflection_nonneg (distributed_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real)
    (h1 : (distributed_load >= (0 : Real)))
    (h2 : (distributed_load <= W_MAX))
    (h3 : (span_length >= L_MIN))
    (h4 : (span_length <= L_MAX))
    (h5 : (modulus >= E_MIN))
    (h6 : (modulus <= E_MAX))
    (h7 : (second_moment >= I_MIN))
    (h8 : (second_moment <= I_MAX)) :
    ((deflection_simple_udl distributed_load span_length modulus second_moment) >= (0 : Real)) := by
  unfold deflection_simple_udl
  try unfold ONE at *
  try unfold THREE at *
  try unfold FOURTY_EIGHT at *
  try unfold FIVE at *
  try unfold THREE_EIGHTY_FOUR at *
  try unfold F_MAX at *
  try unfold W_MAX at *
  try unfold L_MIN at *
  try unfold L_MAX at *
  try unfold E_MIN at *
  try unfold E_MAX at *
  try unfold I_MIN at *
  try unfold I_MAX at *
  try unfold C_MAX at *
  try unfold M_MAX at *
  try mach_split_hyps
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover

-- ── deflection_cantilever_end ──

noncomputable def deflection_cantilever_end (point_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real) : Real :=
  ((((point_load * span_length) * span_length) * span_length) / ((THREE * modulus) * second_moment))

-- source obligations for deflection_cantilever_end: {O1}
--   O1 [826cb0ac51a5] -> PRESERVED (accounted)  theorem cantilever_end_load_deflection_nonneg
--        build: unconditional -- the theorem STATEMENT is in the artifact unconditionally; whether it is PROVED is a separate axis -- this checker rejects an undischarged theorem unless cheating is explicitly enabled
theorem cantilever_end_load_deflection_nonneg (point_load : Real) (span_length : Real) (modulus : Real) (second_moment : Real)
    (h1 : (point_load >= (0 : Real)))
    (h2 : (point_load <= F_MAX))
    (h3 : (span_length >= L_MIN))
    (h4 : (span_length <= L_MAX))
    (h5 : (modulus >= E_MIN))
    (h6 : (modulus <= E_MAX))
    (h7 : (second_moment >= I_MIN))
    (h8 : (second_moment <= I_MAX)) :
    ((deflection_cantilever_end point_load span_length modulus second_moment) >= (0 : Real)) := by
  unfold deflection_cantilever_end
  try unfold ONE at *
  try unfold THREE at *
  try unfold FOURTY_EIGHT at *
  try unfold FIVE at *
  try unfold THREE_EIGHTY_FOUR at *
  try unfold F_MAX at *
  try unfold W_MAX at *
  try unfold L_MIN at *
  try unfold L_MAX at *
  try unfold E_MIN at *
  try unfold E_MAX at *
  try unfold I_MIN at *
  try unfold I_MAX at *
  try unfold C_MAX at *
  try unfold M_MAX at *
  try mach_split_hyps
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover

-- ── bending_stress ──

noncomputable def bending_stress (moment : Real) (distance_to_fiber : Real) (second_moment : Real) : Real :=
  ((moment * distance_to_fiber) / second_moment)

-- obligations for bending_stress: none declared (this artifact proves well-typedness only)
-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem bending_stress_signed_with_moment (moment : Real) (distance_to_fiber : Real) (second_moment : Real)
    (h1 : (moment >= (-M_MAX)))
    (h2 : (moment <= M_MAX))
    (h3 : (distance_to_fiber >= (0 : Real)))
    (h4 : (distance_to_fiber <= C_MAX))
    (h5 : (second_moment >= I_MIN))
    (h6 : (second_moment <= I_MAX)) :
    True := by
  trivial
