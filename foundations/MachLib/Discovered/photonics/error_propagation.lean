-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/photonics/tolerance/error_propagation.eml
-- P3 Manufacturing Tolerance (2/4)

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def N_MZI : Real := (6.0 : Real)

axiom correlated_total_error (delta_per_mzi : Real) (n : Real) : Real
axiom independent_total_error_sq (sigma_per_mzi : Real) (n : Real) : Real

-- ── zero_per_mzi_certificate ──

noncomputable def zero_per_mzi_certificate (n : Real) : Real :=
  (0 : Real)

theorem error_propagation_zero_per_mzi_zero_total (n : Real) :
    ((zero_per_mzi_certificate n) = (0 : Real)) := by
  unfold zero_per_mzi_certificate
  rfl

-- ── correlated_nonneg_certificate ──

noncomputable def correlated_nonneg_certificate (delta : Real) (n : Real) : Real :=
  (delta * n)

theorem error_propagation_correlated_total_nonneg (delta : Real) (n : Real)
    (h1 : (delta >= (0 : Real)))
    (h2 : (n >= (0 : Real))) :
    ((correlated_nonneg_certificate delta n) >= (0 : Real)) := by
  unfold correlated_nonneg_certificate
  exact mul_nonneg h1 h2

-- ── zero_mzis_certificate ──

noncomputable def zero_mzis_certificate (delta : Real) : Real :=
  (0 : Real)

theorem error_propagation_zero_mzis_zero_total (delta : Real) :
    ((zero_mzis_certificate delta) = (0 : Real)) := by
  unfold zero_mzis_certificate
  rfl
