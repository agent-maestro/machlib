-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/photonics/components/waveguide.eml
-- P1 Photonic Component Library (1/8)

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def ALPHA_DEFAULT : Real := (0.5 : Real)

axiom intensity (z : Real) (i_in : Real) (alpha : Real) : Real

-- ── intensity_at_origin ──

noncomputable def intensity_at_origin (i_in : Real) (alpha : Real) : Real :=
  i_in

theorem waveguide_intensity_at_origin (i_in : Real) (alpha : Real) :
    ((intensity_at_origin i_in alpha) = i_in) := by
  unfold intensity_at_origin
  rfl

-- ── loss_positive ──

noncomputable def loss_positive (alpha : Real) : Real :=
  alpha

theorem waveguide_loss_positive (alpha : Real)
    (h1 : (alpha > (0 : Real))) :
    ((loss_positive alpha) > (0 : Real)) := by
  unfold loss_positive
  exact h1
