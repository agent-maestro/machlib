-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/photonics/hybrid/latency_model.eml
-- P4 Photonic-Electronic Co-Design (2/3)

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def T_PHOTONIC_NS : Real := (0.5 : Real)
noncomputable def T_ELECTRONIC_US : Real := (10.0 : Real)
noncomputable def T_CONVERSION_NS : Real := (5.0 : Real)

axiom total_latency_ns (t_photonic_ns : Real) (t_electronic_ns : Real) (t_conversion_ns : Real) : Real

-- ── zero_stage_certificate ──

noncomputable def zero_stage_certificate  : Real :=
  (0 : Real)

theorem latency_zero_stages_zero_total  :
    ((zero_stage_certificate ) = (0 : Real)) := by
  unfold zero_stage_certificate
  rfl

-- ── total_nonneg_certificate ──
-- a + b + c >= 0  via add_nonneg chain.

noncomputable def total_nonneg_certificate (a : Real) (b : Real) (c : Real) : Real :=
  ((a + b) + c)

theorem latency_total_nonneg (a : Real) (b : Real) (c : Real)
    (h1 : (a >= (0 : Real)))
    (h2 : (b >= (0 : Real)))
    (h3 : (c >= (0 : Real))) :
    ((total_nonneg_certificate a b c) >= (0 : Real)) := by
  unfold total_nonneg_certificate
  exact add_nonneg (add_nonneg h1 h2) h3
