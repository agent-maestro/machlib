-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/photonics/components/mach_zehnder.eml
-- P1 Photonic Component Library (7/8)

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

axiom through (i_in : Real) (delta_phi : Real) : Real
axiom drop (i_in : Real) (delta_phi : Real) : Real

-- ── through_at_zero_phase ──

noncomputable def through_at_zero_phase (i_in : Real) : Real :=
  i_in

theorem mzi_through_at_zero_phase (i_in : Real) :
    ((through_at_zero_phase i_in) = i_in) := by
  unfold through_at_zero_phase
  rfl

-- ── drop_at_zero_phase ──

noncomputable def drop_at_zero_phase (i_in : Real) : Real :=
  (0 : Real)

theorem mzi_drop_at_zero_phase (i_in : Real) :
    ((drop_at_zero_phase i_in) = (0 : Real)) := by
  unfold drop_at_zero_phase
  rfl

-- ── energy_witness ──
-- sin^2(theta) + cos^2(theta) = 1.  Closes via pythagorean.

noncomputable def energy_witness (theta : Real) : Real :=
  (((Real.sin theta) * (Real.sin theta)) + ((Real.cos theta) * (Real.cos theta)))

theorem mzi_energy_conserved (theta : Real) :
    ((energy_witness theta) = (1 : Real)) := by
  unfold energy_witness
  exact pythagorean theta
