-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/photonics/components/directional_coupler.eml
-- P1 Photonic Component Library (6/8)

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def KAPPA_DEFAULT : Real := (1000.0 : Real)

axiom p_cross (kappa : Real) (length : Real) : Real
axiom p_bar (kappa : Real) (length : Real) : Real

-- ── p_bar_at_zero ──
-- cos(0)*cos(0) folds to 1 in the optimiser; ensures matches.

noncomputable def p_bar_at_zero  : Real :=
  (1 : Real)

theorem directional_coupler_bar_at_zero  :
    ((p_bar_at_zero ) = (1 : Real)) := by
  unfold p_bar_at_zero
  rfl

-- ── p_cross_at_zero ──
-- sin(0)*sin(0) folds to 0.

noncomputable def p_cross_at_zero  : Real :=
  (0 : Real)

theorem directional_coupler_cross_at_zero  :
    ((p_cross_at_zero ) = (0 : Real)) := by
  unfold p_cross_at_zero
  rfl

-- ── energy_witness ──
-- sin^2 + cos^2 = 1 -- direct application of the Pythagorean axiom.
-- The killer obligation: energy conservation through the coupler.

noncomputable def energy_witness (theta : Real) : Real :=
  (((Real.sin theta) * (Real.sin theta)) + ((Real.cos theta) * (Real.cos theta)))

theorem directional_coupler_energy_conserved (theta : Real) :
    ((energy_witness theta) = (1 : Real)) := by
  unfold energy_witness
  exact pythagorean theta
