-- Generated by the EML-lang Lean backend
-- Source module: gaussian_peak
-- Source file:   <private>/gaussian_peak.eml
-- Verified fns:  gaussian_density

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def SQRT_2PI : Real := (2.5066282746310002 : Real)
noncomputable def SIGMA_MAX : Real := (1000000.0 : Real)

axiom fwhm_to_sigma (fwhm : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

axiom gaussian_area_normalised (frequency : Real) (centre : Real) (sigma : Real) (area : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── gaussian_density ──

noncomputable def gaussian_density (frequency : Real) (centre : Real) (sigma : Real) : Real :=
  ((Real.exp (((-(frequency - centre)) * (frequency - centre)) / (((2.0 : Real) * sigma) * sigma))) / (sigma * SQRT_2PI))

theorem gaussian_peak_at_centre (frequency : Real) (centre : Real) (sigma : Real)
    (h1 : (sigma > (0 : Real)))
    (h2 : (sigma <= SIGMA_MAX)) :
    ((gaussian_density frequency centre sigma) > (0 : Real)) := by
  unfold gaussian_density
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover
