-- Generated by the EML-lang Lean backend, then sorry-closed by hand
-- against MachLib.Forge.
-- Source module: maglev_controller
-- Source file:   forge/examples/maglev/controller.eml
-- Verified fn:   proportional_at_zero
--
-- Phase E5 of the math-to-manufactured-PCB pipeline. The
-- zero-input -> zero-output property of the proportional path
-- closes by definitional unfolding (the EML constant folder
-- already collapsed `KP * 0.0` to `(0 : Real)`).
--
-- The output-bounded property of the full clamped PID is
-- intentionally NOT a Lean obligation in this v1 -- closing it
-- requires min/max lemmas not yet in MachLib.Forge. Flagged in
-- examples/proofs/README.md.

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def KP : Real := (2.5 : Real)
noncomputable def KI : Real := (0.4 : Real)
noncomputable def KD : Real := (0.05 : Real)
noncomputable def OUT_MIN : Real := (-(1 : Real))
noncomputable def OUT_MAX : Real := (1 : Real)

axiom circuit  : Real
axiom pid (error : Real) (integral : Real) (derivative : Real) : Real
axiom proportional (error : Real) : Real
axiom raw_pid_sum (error : Real) (integral : Real) (derivative : Real) : Real

-- ── proportional_at_zero ──

noncomputable def proportional_at_zero  : Real :=
  (0 : Real)

theorem controller_zero_input_zero_output  :
    ((proportional_at_zero ) = (0 : Real)) := by
  unfold proportional_at_zero
  rfl
