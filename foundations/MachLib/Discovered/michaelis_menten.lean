-- Generated by the EML-lang Lean backend
-- Source module: michaelis_menten
-- Source file:   <private>/michaelis_menten.eml
-- Verified fns:  velocity

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def S_MAX : Real := (1000.0 : Real)
noncomputable def VMAX_MAX : Real := (1000000.0 : Real)

axiom lineweaver_burk (velocity_obs : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

axiom eadie_hofstee (velocity_obs : Real) (substrate : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── velocity ──

noncomputable def velocity (vmax : Real) (substrate : Real) (km : Real) : Real :=
  ((vmax * substrate) / (km + substrate))

theorem michaelis_menten_saturating (vmax : Real) (substrate : Real) (km : Real)
    (h1 : (vmax > (0 : Real)))
    (h2 : (vmax <= VMAX_MAX))
    (h3 : (substrate >= (0 : Real)))
    (h4 : (substrate <= S_MAX))
    (h5 : (km > (0 : Real)))
    (h6 : (km <= S_MAX)) :
    (((velocity vmax substrate km) >= (0 : Real))) ∧ (((velocity vmax substrate km) <= vmax)) := by
  unfold velocity
  refine ⟨?_, ?_⟩ <;>
    first
    | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
    | apply clamp_le_hi
    | mach_positivity
    | mach_sign
    | (apply convex_comb_le <;> assumption)
    | (apply convex_comb_ge <;> assumption)
    | (apply convex_comb3_le <;> assumption)
    | (apply convex_comb3_ge <;> assumption)
    | (apply convex_comb4_le <;> assumption)
    | (apply convex_comb4_ge <;> assumption)
    | (apply convex_comb5_le <;> assumption)
    | (apply convex_comb5_ge <;> assumption)
    | (apply convex_comb6_le <;> assumption)
    | (apply convex_comb6_ge <;> assumption)
    | rfl
    | sorry  -- out of reach; left for the prover
