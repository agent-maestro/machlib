-- Generated by the EML-lang Lean backend
-- Source module: swing_equation
-- Source file:   <private>/swing_equation.eml
-- Verified fns:  rotor_angle_step, rotor_speed_step, power_angle_curve

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def M_MIN : Real := (0.1 : Real)
noncomputable def M_MAX : Real := (100.0 : Real)
noncomputable def D_MIN : Real := (0 : Real)
noncomputable def D_MAX : Real := (50.0 : Real)
noncomputable def POWER_MAX : Real := (100.0 : Real)
noncomputable def OMEGA_MAX : Real := (50.0 : Real)
noncomputable def DELTA_MAX : Real := (6.2832 : Real)
noncomputable def DT_MAX : Real := (0.1 : Real)

-- ── rotor_angle_step ──

noncomputable def rotor_angle_step (delta_prev : Real) (omega : Real) (dt : Real) : Real :=
  (delta_prev + (omega * dt))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem rotor_angle_step_linear_in_dt (delta_prev : Real) (omega : Real) (dt : Real)
    (h1 : ((abs delta_prev) <= DELTA_MAX))
    (h2 : ((abs omega) <= OMEGA_MAX))
    (h3 : (dt >= (0 : Real)))
    (h4 : (dt <= DT_MAX)) :
    True := by
  trivial

-- ── rotor_speed_step ──

noncomputable def rotor_speed_step (omega_prev : Real) (p_mechanical : Real) (p_electrical : Real) (inertia : Real) (damping : Real) (dt : Real) : Real :=
  (omega_prev + ((dt * ((p_mechanical - p_electrical) - (damping * omega_prev))) / inertia))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem rotor_speed_step_balances_power (omega_prev : Real) (p_mechanical : Real) (p_electrical : Real) (inertia : Real) (damping : Real) (dt : Real)
    (h1 : ((abs omega_prev) <= OMEGA_MAX))
    (h2 : ((abs p_mechanical) <= POWER_MAX))
    (h3 : ((abs p_electrical) <= POWER_MAX))
    (h4 : (inertia >= M_MIN))
    (h5 : (inertia <= M_MAX))
    (h6 : (damping >= D_MIN))
    (h7 : (damping <= D_MAX))
    (h8 : (dt >= (0 : Real)))
    (h9 : (dt <= DT_MAX)) :
    True := by
  trivial

-- ── power_angle_curve ──

noncomputable def power_angle_curve (internal_emf : Real) (bus_voltage : Real) (reactance : Real) (rotor_angle : Real) : Real :=
  (((internal_emf * bus_voltage) * (Real.sin rotor_angle)) / reactance)

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem power_angle_zero_at_zero_delta (internal_emf : Real) (bus_voltage : Real) (reactance : Real) (rotor_angle : Real)
    (h1 : (internal_emf >= (0 : Real)))
    (h2 : (internal_emf <= (5.0 : Real)))
    (h3 : (bus_voltage >= (0 : Real)))
    (h4 : (bus_voltage <= (5.0 : Real)))
    (h5 : (reactance >= (0.01 : Real)))
    (h6 : (reactance <= (10.0 : Real)))
    (h7 : ((abs rotor_angle) <= DELTA_MAX)) :
    True := by
  trivial
