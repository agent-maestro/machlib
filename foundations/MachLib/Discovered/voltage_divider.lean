-- Generated by the EML-lang Lean backend, then sorry-closed by hand
-- against MachLib.Forge.
-- Source module: voltage_divider
-- Source file:   forge/examples/voltage_divider.eml
-- Verified fns:  vout_divider, rsum, vout_symmetric
--
-- Phase E4 of the math-to-manufactured-PCB pipeline. Each theorem
-- below is the structural certificate the Lean backend emits as a
-- `sorry` from the `@verify(lean, theorem = "...")` annotations on
-- the source EML file; the bodies here close those obligations
-- against MachLib's axiomatic real-number layer.

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

axiom circuit  : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── vout_divider ──

noncomputable def vout_divider (vin : Real) (r1 : Real) (r2 : Real) : Real :=
  ((vin * r2) / (r1 + r2))

theorem voltage_divider_law (vin : Real) (r1 : Real) (r2 : Real)
    (h1 : (r1 > (0 : Real)))
    (h2 : (r2 > (0 : Real))) :
    ((vout_divider vin r1 r2) = ((vin * r2) / (r1 + r2))) := by
  unfold vout_divider
  rfl

-- ── rsum ──

noncomputable def rsum (r1 : Real) (r2 : Real) : Real :=
  (r1 + r2)

theorem voltage_divider_denom_pos (r1 : Real) (r2 : Real)
    (h1 : (r1 > (0 : Real)))
    (h2 : (r2 > (0 : Real))) :
    ((rsum r1 r2) > (0 : Real)) := by
  unfold rsum
  exact add_pos h1 h2

-- ── vout_symmetric ──

noncomputable def vout_symmetric (vin : Real) (r : Real) : Real :=
  ((vin * r) / (r + r))

theorem voltage_divider_symmetric_half (vin : Real) (r : Real)
    (h1 : (r > (0 : Real))) :
    ((vout_symmetric vin r) = ((vin * r) / (r + r))) := by
  unfold vout_symmetric
  rfl
