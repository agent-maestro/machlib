-- Generated by the EML-lang Lean backend
-- Source module: ph_control
-- Source file:   <private>/ph_control.eml
-- Verified fns:  pH_from_concentration, concentration_from_pH

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def PH_MIN : Real := (-(2.0 : Real))
noncomputable def PH_MAX : Real := (16.0 : Real)
noncomputable def C_MIN : Real := (1e-15 : Real)
noncomputable def C_MAX : Real := (1000.0 : Real)

axiom henderson_hasselbalch (pka : Real) (conjugate_base_concentration : Real) (weak_acid_concentration : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── pH_from_concentration ──

noncomputable def pH_from_concentration (proton_concentration : Real) : Real :=
  (-(log10 proton_concentration))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem ph_decreases_with_proton_concentration (proton_concentration : Real)
    (h1 : (proton_concentration >= C_MIN))
    (h2 : (proton_concentration <= C_MAX)) :
    True := by
  trivial

-- ── concentration_from_pH ──

noncomputable def concentration_from_pH (ph_value : Real) : Real :=
  (exp10 (-ph_value))

theorem proton_concentration_positive (ph_value : Real)
    (h1 : (ph_value >= PH_MIN))
    (h2 : (ph_value <= PH_MAX)) :
    ((concentration_from_pH ph_value) > (0 : Real)) := by
  unfold concentration_from_pH
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover
