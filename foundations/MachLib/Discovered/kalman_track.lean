-- Generated by the EML-lang Lean backend
-- Source module: kalman_track
-- Source file:   <private>/kalman_track.eml
-- Verified fns:  predict_position, innovation, kalman_gain

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

axiom update_position (x_pred : Real) (gain : Real) (innov : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

axiom update_variance (gain : Real) (p_pred : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── predict_position ──

noncomputable def predict_position (x_pos : Real) (x_vel : Real) (dt : Real) : Real :=
  (x_pos + (x_vel * dt))

-- obligations for predict_position: none declared (this artifact proves well-typedness only)
-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem track_position_predict_linear (x_pos : Real) (x_vel : Real) (dt : Real)
    (h1 : (dt >= (0 : Real))) :
    True := by
  trivial

-- ── innovation ──

noncomputable def innovation (z_meas : Real) (x_pred : Real) : Real :=
  (z_meas - x_pred)

-- obligations for innovation: none declared (this artifact proves well-typedness only)
-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem track_innovation_zero_when_aligned (z_meas : Real) (x_pred : Real) :
    True := by
  trivial

-- ── kalman_gain ──

noncomputable def kalman_gain (p_pred : Real) (r_meas : Real) : Real :=
  (p_pred / (p_pred + r_meas))

-- source obligations for kalman_gain: {O1, O2}
--   O1 [5365885cbe55] -> PRESERVED (accounted)  theorem track_gain_in_unit_interval
--        build: unconditional -- the theorem STATEMENT is in the artifact unconditionally; whether it is PROVED is a separate axis -- this checker rejects an undischarged theorem unless cheating is explicitly enabled
--   O2 [55757c17dec0] -> PRESERVED (accounted)  theorem track_gain_in_unit_interval
--        build: unconditional -- the theorem STATEMENT is in the artifact unconditionally; whether it is PROVED is a separate axis -- this checker rejects an undischarged theorem unless cheating is explicitly enabled
theorem track_gain_in_unit_interval (p_pred : Real) (r_meas : Real)
    (h1 : (p_pred >= (0 : Real)))
    (h2 : (r_meas > (0 : Real))) :
    (((kalman_gain p_pred r_meas) >= (0 : Real))) ∧ (((kalman_gain p_pred r_meas) <= (1 : Real))) := by
  unfold kalman_gain
  try mach_split_hyps
  refine ⟨?_, ?_⟩ <;>
    first
    | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
    | apply clamp_le_hi
    | mach_positivity
    | mach_sign
    | (apply convex_comb_le <;> assumption)
    | (apply convex_comb_ge <;> assumption)
    | (apply convex_comb3_le <;> assumption)
    | (apply convex_comb3_ge <;> assumption)
    | (apply convex_comb4_le <;> assumption)
    | (apply convex_comb4_ge <;> assumption)
    | (apply convex_comb5_le <;> assumption)
    | (apply convex_comb5_ge <;> assumption)
    | (apply convex_comb6_le <;> assumption)
    | (apply convex_comb6_ge <;> assumption)
    | rfl
    | sorry  -- out of reach; left for the prover
