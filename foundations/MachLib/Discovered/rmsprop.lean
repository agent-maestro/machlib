-- Generated by the EML-lang Lean backend
-- Source module: rmsprop_optimizer
-- Source file:   <private>/rmsprop.eml
-- Verified fns:  rmsprop_v_update, rmsprop_param_step, adagrad_param_step

import MachLib.EML
import MachLib.Trig
import MachLib.Forge
import MachLib.Linarith
import MachLib.FixedPoint
import MachLib.SignTactic

open MachLib
open MachLib.Real

noncomputable def LR_MIN : Real := (1e-09 : Real)
noncomputable def LR_MAX : Real := (1 : Real)
noncomputable def RHO_MAX : Real := (0.9999999 : Real)
noncomputable def VAL_MAX : Real := (1000000.0 : Real)
noncomputable def EPS_MIN : Real := (1e-12 : Real)
noncomputable def EPS_MAX : Real := (0.001 : Real)

-- ── rmsprop_v_update ──

noncomputable def rmsprop_v_update (prev_v : Real) (gradient : Real) (rho : Real) : Real :=
  ((rho * prev_v) + ((((1 : Real) - rho) * gradient) * gradient))

theorem rmsprop_v_nonneg (prev_v : Real) (gradient : Real) (rho : Real)
    (h1 : (prev_v >= (0 : Real)))
    (h2 : (prev_v <= VAL_MAX))
    (h3 : ((abs gradient) <= VAL_MAX))
    (h4 : (rho >= (0 : Real)))
    (h5 : (rho <= RHO_MAX)) :
    ((rmsprop_v_update prev_v gradient rho) >= (0 : Real)) := by
  unfold rmsprop_v_update
  first
  | (apply lo_le_clamp <;> (first | assumption | mach_positivity))
  | apply clamp_le_hi
  | mach_positivity
  | mach_sign
  | (apply convex_comb_le <;> assumption)
  | (apply convex_comb_ge <;> assumption)
  | (apply convex_comb3_le <;> assumption)
  | (apply convex_comb3_ge <;> assumption)
  | (apply convex_comb4_le <;> assumption)
  | (apply convex_comb4_ge <;> assumption)
  | (apply convex_comb5_le <;> assumption)
  | (apply convex_comb5_ge <;> assumption)
  | (apply convex_comb6_le <;> assumption)
  | (apply convex_comb6_ge <;> assumption)
  | rfl
  | sorry  -- out of reach; left for the prover

-- ── rmsprop_param_step ──

noncomputable def rmsprop_param_step (theta : Real) (gradient : Real) (v : Real) (lr : Real) (eps : Real) : Real :=
  (theta - ((lr * gradient) / ((Real.sqrt v) + eps)))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem rmsprop_step_descent_when_grad_signed (theta : Real) (gradient : Real) (v : Real) (lr : Real) (eps : Real)
    (h1 : ((abs theta) <= VAL_MAX))
    (h2 : ((abs gradient) <= VAL_MAX))
    (h3 : (v >= (0 : Real)))
    (h4 : (v <= VAL_MAX))
    (h5 : (lr >= LR_MIN))
    (h6 : (lr <= LR_MAX))
    (h7 : (eps >= EPS_MIN))
    (h8 : (eps <= EPS_MAX)) :
    True := by
  trivial

-- ── adagrad_param_step ──

noncomputable def adagrad_param_step (theta : Real) (gradient : Real) (g_squared_sum : Real) (lr : Real) (eps : Real) : Real :=
  (theta - ((lr * gradient) / ((Real.sqrt g_squared_sum) + eps)))

-- ⚠ NO OBLIGATION: kernel declares no `ensures` and no return
-- refinement, so this theorem is vacuously `True` (proves only
-- well-typedness). Exclude from any close-rate / verified count.
theorem adagrad_step_decreasing_lr_effective (theta : Real) (gradient : Real) (g_squared_sum : Real) (lr : Real) (eps : Real)
    (h1 : ((abs theta) <= VAL_MAX))
    (h2 : ((abs gradient) <= VAL_MAX))
    (h3 : (g_squared_sum >= (0 : Real)))
    (h4 : (g_squared_sum <= VAL_MAX))
    (h5 : (lr >= LR_MIN))
    (h6 : (lr <= LR_MAX))
    (h7 : (eps >= EPS_MIN))
    (h8 : (eps <= EPS_MAX)) :
    True := by
  trivial
