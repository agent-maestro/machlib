-- Generated by the EML-lang Lean backend, then sorry-closed by hand.
-- Source: forge/examples/carriers/quantum/hadamard.eml
-- Tier 7: QUANTUM

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

axiom h_norm_2x_squared_unnormalised (a : Real) (b : Real) : Real
axiom twice_input_norm (a : Real) (b : Real) : Real

-- ── h0_norm_unnormalised ──

noncomputable def h0_norm_unnormalised  : Real :=
  (2.0 : Real)

theorem hadamard_basis_zero_norm_preserved  :
    ((h0_norm_unnormalised ) = (2.0 : Real)) := by
  unfold h0_norm_unnormalised
  rfl

-- ── h1_norm_unnormalised ──

noncomputable def h1_norm_unnormalised  : Real :=
  (2.0 : Real)

theorem hadamard_basis_one_norm_preserved  :
    ((h1_norm_unnormalised ) = (2.0 : Real)) := by
  unfold h1_norm_unnormalised
  rfl

-- ── squared_norm_witness ──

noncomputable def squared_norm_witness (a : Real) : Real :=
  (a * a)

theorem hadamard_squared_norm_nonneg_witness (a : Real) :
    ((squared_norm_witness a) = (a * a)) := by
  unfold squared_norm_witness
  rfl
