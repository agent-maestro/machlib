-- Generated by the EML-lang Lean backend
-- Source module: two_compartment
-- Source file:   <private>/two_compartment.eml
-- Verified fns:  plasma_concentration

import MachLib.EML
import MachLib.Trig
import MachLib.Forge

open MachLib
open MachLib.Real

noncomputable def T_MAX : Real := (1000.0 : Real)
noncomputable def RATE_MAX : Real := (100.0 : Real)

axiom initial_concentration (a_coeff : Real) (b_coeff : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

axiom auc_inf (a_coeff : Real) (alpha_rate : Real) (b_coeff : Real) (beta_rate : Real) : Real  -- helper (axiomatised in MachLib/Discovered)

-- ── plasma_concentration ──

noncomputable def plasma_concentration (a_coeff : Real) (alpha_rate : Real) (b_coeff : Real) (beta_rate : Real) (time : Real) : Real :=
  ((a_coeff * (Real.exp ((-alpha_rate) * time))) + (b_coeff * (Real.exp ((-beta_rate) * time))))

theorem two_compartment_alpha_dominates_early (a_coeff : Real) (alpha_rate : Real) (b_coeff : Real) (beta_rate : Real) (time : Real)
    (h1 : (a_coeff >= (0 : Real)))
    (h2 : (b_coeff >= (0 : Real)))
    (h3 : (alpha_rate >= (0 : Real)))
    (h4 : (alpha_rate <= RATE_MAX))
    (h5 : (beta_rate >= (0 : Real)))
    (h6 : (beta_rate <= RATE_MAX))
    (h7 : (time >= (0 : Real)))
    (h8 : (time <= T_MAX)) :
    ((plasma_concentration a_coeff alpha_rate b_coeff beta_rate time) >= (0 : Real)) := by
  unfold plasma_concentration
  sorry  -- TODO: prove against MachLib foundations
