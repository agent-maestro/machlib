# Auto-generated by `auto_prove.py` from forge .eml kernels.
# Per the standing rule, the user verifies in VS Code before any
# public push. Until then, these files live unignored locally so
# the auto_prove driver can re-emit them, but staged for review.
*
!.gitignore
