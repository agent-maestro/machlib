# Qwen CapCard Next Steps

1. Finish installing `qwen3-coder:30b` or another suitable local coding model during a dedicated model-install window.
2. Re-run `run-bakeoff` with real model execution enabled when the runner policy is switched from scorer-only to real local outputs.
3. Use the prompt pack to generate direct candidate repairs for rows 2 and 3.
4. Add direct repaired row artifacts plus validator PASS output before considering internal marketplace approval.
5. Consider fine-tune dataset review only after human review confirms examples are clean and useful.
