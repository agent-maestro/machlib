# Qwen CapCard Failure Memory V2

- Pattern: `qwen3:30b` is callable but too slow for strict 3-second CapCard JSON attempts.
- Pattern: thinking text can precede JSON, so extraction is mandatory.
- Pattern: row 2/3 remain evidence-blocked.
