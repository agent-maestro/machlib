# Qwen CapCard Failure Memory v3

- Worst patterns: `["local model may still timeout", "warn/unknown rows remain evidence-blocked"]`.
- Best repair patterns: `["schema format lowered extraction noise", "explicit missing evidence stayed bounded"]`.
